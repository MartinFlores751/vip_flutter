class User {
  int userId;
  int id;
  var fullName;
  var userName;
  var password;
  var conPass;
  int helper;

  User(
      {this.fullName, this.userName, this.password, this.conPass, this.helper});
}
